{
  "name": "fastbridge-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:root": "turbo run dev --filter=root",
    "dev:all": "turbo run dev --parallel --filter=@fastbridge/monad --filter=@fastbridge/megaeth --filter=root",
    "build:all": "turbo run build",
    "clean": "turbo run clean --force"
  },
  "packageManager": "pnpm@9.0.0",
  "devDependencies": {
    "turbo": "^2.7.5"
  }
}
